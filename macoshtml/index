
<!DOCTYPE html>
<html>
<head>
  <title>macOS 窗口示例</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
      font-family: Arial, sans-serif;
    }
    
    .window {
      width: 800px;
      height: 500px;
      background-color: #fefefe;
      border-radius: 8px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      margin: 50px auto;
      display: flex;
      flex-direction: column;
    }
    
    .title-bar {
      display: flex;
      align-items: center;
      height: 35px;
      background-color: #f0f0f0;
      border-radius: 8px 8px 0 0;
      padding: 0 10px;
      user-select: none;
    }
    
    .traffic-buttons {
      display: flex;
      width: 70px;
      justify-content: space-between;
    }
    
    .traffic-button {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    
    .red-button {
      background-color: #ff5f57;
    }
    
    .yellow-button {
      background-color: #ffbd2e;
    }
    
    .green-button {
      background-color: #28c940;
    }
    
    .title-bar .title {
      flex-grow: 1;
      font-weight: bold;
      margin-left: 10px;
      user-select: none;
    }
    
    .content {
      flex-grow: 1;
      padding: 10px;
    }
  </style>
  <script>
    window.onload = function() {
      var backButton = document.getElementById('back-button');
      var zoomButton = document.getElementById('zoom-button');
      
      var isZoomed = false;
      var previousURL = '';
      
      backButton.addEventListener('click', function() {
        if (previousURL) {
          window.location.href = previousURL;
        }
      });
      
      zoomButton.addEventListener('click', function() {
        var windowElement = document.querySelector('.window');
        if (isZoomed) {
          windowElement.style.transform = 'scale(1)';
          isZoomed = false;
        } else {
          windowElement.style.transform = 'scale(1.2)';
          isZoomed = true;
        }
      });
    };
  </script>
</head>
<body>
  <div class="window">
    <div class="title-bar">
      <div class="traffic-buttons">
        <div id="back-button" class="traffic-button red-button"></div>
        <div class="traffic-button yellow-button"></div>
        <div id="zoom-button" class="traffic-button green-button"></div>
      </div>
      <div class="title">模拟macOS窗口打开网页</div>
    </div>
    <div class="content">
      <iframe src="https://bananayx.github.io/macoshtml" width="100%" height="100%" frameborder="0"></iframe>
    </div>
  </div>
</body>
</html>
